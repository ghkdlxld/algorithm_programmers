SELECT fh.FLAVOR
FROM FIRST_HALF fh 
LEFT JOIN (SELECT SHIPMENT_ID, FLAVOR, SUM(TOTAL_ORDER) AS TOTAL_ORDER FROM JULY GROUP BY FLAVOR) j
ON fh.SHIPMENT_ID = j.SHIPMENT_ID
GROUP BY fh.FLAVOR
ORDER BY SUM(fh.TOTAL_ORDER) + SUM(j.TOTAL_ORDER) DESC LIMIT 3;